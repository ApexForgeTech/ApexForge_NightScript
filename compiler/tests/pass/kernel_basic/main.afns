package kernel.hello;

extern "C" fn puts(s: cstr) -> i32;

kernel app HelloKernel {
    fn helper(x: i32) -> i32 {
        return x + 1;
    }

    fn main() -> void {
        let v: i32 = helper(41);
        puts("ok");
        if v == 42 { puts("kernel ok"); }
    }
}
