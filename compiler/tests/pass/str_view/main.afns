package test;

extern "C" fn puts(s: cstr) -> i32;

fn main() -> i32 {
    let s: str = "abc";
    let first: u8 = s.ptr[0];

    if s.len == 3 && first == 97 {
        puts("ok");
    }

    return 0;
}
